# Poiem visual reset — phase 1 review pack

Approved Today plus the shared foundations. Unmigrated screens keep their current values.

## What landed

- Baseline audit: [`DESIGN-BASELINE.md`](../DESIGN-BASELINE.md), regenerated by `node scripts/audit-styles.mjs`.
- Determinism: self-hosted Barlow Condensed, Plus Jakarta Sans and IBM Plex Mono; `e2e/seed.ts` writes a fixed account (Ada, three meals, one weigh-in); clock frozen at `2026-09-19T20:00:00.000Z`; mascot RNG seeded and overlay hidden for captures.
- Additive contract tokens in `styles/system/tokens.css`. Welcome poster sheets were **not** deleted — JavaScript still imports them.
- Shared primitives in `components/system/` and the `/dev/components` reference page.
- Today rebuilt on those primitives in `styles/screens/today.css`. One acid hero, quieter macros, one meal-row grid, upright icons, one divider. Momo docks via `TodayMomo`. Saved and Insights are now quiet.
- Visual project: 320 / 390 / 768 / 1440 × light / dark for Today and the reference page. `npm run visual` and `visual:update` run inside `mcr.microsoft.com/playwright:v1.61.1-noble`. The `web-e2e` CI job uses the same image.

## Contact sheet

Open [`contact-sheet.html`](contact-sheet.html). Before images in [`before/`](before/) were captured from `origin/main` (`5c10f82b`) in the same Playwright container, with the same Ada seed. After images are the committed baselines.

## Component state matrix

`/dev/components` shows:

| Component | Default | Focus | Disabled | Error | Loading |
|-----------|---------|-------|----------|-------|---------|
| Surface | plain / outlined / hero | — | — | — | — |
| PressableButton | primary, secondary, ghost, delete | keyboard focus | Disabled action / Disabled link | — | Loading |
| FormField | hint | demo focus | disabled input | invalid calories | — |
| FilterGroup | selected All | demo Focus chip | Disabled filters | — | — |
| MealRow | oats row | — | — | — | — |
| EmptyState | table is ready | — | — | — | — |
| Sheet | closed | Open sheet | — | — | — |
| NutritionFields | reused unchanged | — | — | — | — |

Light and dark, and 320–1440, are the visual project — not in-page toggles.

## Deferred to phases 2–5

- Log sheet, five logging forms, Saved filters (phase 2).
- Remaining screens and legacy selector removal (phase 3).
- Desktop compositions per task (phase 4).
- Motion polish, full visual matrix, staging sign-off (phase 5).
- `--k-shell: 480px` and hard-coded 480px leftovers.
- The 183 legacy tokens, including unused `--display` Fredoka fallbacks.
- Welcome poster CSS stays until the marketing page moves.

## Test results

- `npm test` in `web/app`: **79 files, 651 tests passed**.
- `npm run lint`: existing warnings only; new audit-script unused symbols removed.
- `web/test/api/browser-headers.test.ts`: passed after dropping Google Fonts from the app CSP.
- `npm run visual:update` inside `mcr.microsoft.com/playwright:v1.61.1-noble`: **17 passed** (16 screenshots + Today alignment). Baselines are in `e2e/visual/__screenshots__/`.
